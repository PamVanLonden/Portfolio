{
    "name": "portfolio-rendered",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "frontend",
      "backend-art"
    ],
    "scripts": {
      "start": "concurrently \"npm run start --prefix backend-art\" \"npm run dev --prefix frontend\"",
      "build": "npm run build --prefix frontend",
      "install-all": "npm install --prefix frontend && npm install --prefix backend-art"
    },
    "devDependencies": {
      "concurrently": "^7.0.0"
    }
  }

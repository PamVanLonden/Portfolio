{
  "name": "art-documents",
  "version": "1.0.0",
  "description": "Art logs using Mongo and Mongoose",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "concurrently \"npm run start --prefix server\" \"npm run build --prefix client\"",
    "build": "npm run build --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\""
  },
  "author": "Pam Van Londen",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.9.1",
    "rest": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "concurrently": "^6.2.0"
  }
}
